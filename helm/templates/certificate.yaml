apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ $.Values.Domain }}
  namespace: {{ $.Values.namespace }}
spec:
  dnsNames:
    - {{ $.Values.Domain }}
  secretName: {{ $.Values.Domain }}
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer

